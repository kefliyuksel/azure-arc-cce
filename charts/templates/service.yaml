apiVersion: v1
kind: Service
metadata:
  name: {{ include "ccechart.fullname" . }}
  labels:
    {{- include "ccechart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ccechart.selectorLabels" . | nindent 4 }}
