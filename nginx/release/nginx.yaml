apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: azure-arc-cce-nginx
  namespace: azure-arc-cce
  annotations:
    clusterconfig.azure.com/use-managed-source: "true"
spec:
  interval: 1m
  releaseName: nginx
  chart:
    spec:
      chart: ./nginx/chart
  values:
    controller:
      image:
        repository: nginx
        tag: "latest"
      replicaCount: 3